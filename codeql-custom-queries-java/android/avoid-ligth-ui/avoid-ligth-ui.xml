<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 🚫 Noncompliant - explicit Light theme -->
    <style name="AppTheme.Light" parent="Theme.AppCompat.Light">
    </style>

    <!-- 🚫 Noncompliant - Material Light theme -->
    <style name="AppTheme.MaterialLight" parent="Theme.MaterialComponents.Light">
    </style>

    <!-- 🚫 Noncompliant - Light theme with DarkActionBar variant -->
    <style name="AppTheme.LightDark" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <!-- 🚫 Noncompliant - Material Light theme with DarkActionBar -->
    <style name="AppTheme.MaterialLightDark" parent="Theme.MaterialComponents.Light.DarkActionBar">
    </style>

    <!-- ✅ Compliant - dark theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat">
    </style>

    <!-- ✅ Compliant - Material dark theme -->
    <style name="AppTheme.MaterialDark" parent="Theme.MaterialComponents">
    </style>

    <!-- ✅ Compliant - DayNight theme (adapts automatically) -->
    <style name="AppTheme.DayNight" parent="Theme.MaterialComponents.DayNight">
    </style>

    <!-- ✅ Compliant - no parent attribute -->
    <style name="AppTheme.Custom">
    </style>

</resources>